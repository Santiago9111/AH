class Product {
    products = [];

    constructor(products=[]){
        this.products = products;
    }

    addProduct( Product ){
        if( producto.name === ""){

        }
        const id = crypto.randomUUID();
        console.log(id);
    }
    getProducts(){

    }
    getProductsById(id){
        
    }
}

module.exports = Product;